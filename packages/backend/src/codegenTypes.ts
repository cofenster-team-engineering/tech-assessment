// This file provides the types used by codegen for mapping TS types to GraphQL types

import type { Project } from './data/projectRepository';
import type { User } from './data/userRepository';

export type UserEntity = User;
export type ProjectEntity = Project;
